apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shinysim-data
  namespace: shinysim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path