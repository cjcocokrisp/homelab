apiVersion: v1
kind: Service
metadata:
  name: convertx-svc
  namespace: convertx
  labels:
    app: convertx
spec:
  type: NodePort
  selector:
    app: convertx
  ports:
    - port: 3000
      targetPort: 3000
